[package]
name = "revm-runner"
version = "0.1.0"
edition = "2021"

# revm is the EVM engine used by reth, so this measures "reth's EVM".
[dependencies]
revm = "14"
hex = "0.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
